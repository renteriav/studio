<% provide(:title, 'Update Sharing Enrollment') %>
<h1>Editing sharing sign up</h1>
<h2><%= "#{@student.first} #{@student.last}" %></h2>
<%= select_date @date, {:order => [:year, :month], :use_short_month => true, :end_year => Time.now.year}, {:class => "input-sm sharing-search"} %>
<input type="hidden" id="hidden_field" value='<%= "#{@student.id}" %>' />
<input type="hidden" id="hidden_field_2" value='<%= "#{@detailed_sharing.id}" %>' />
<div id = "sharing-result">
	<%= render file: 'detailed_sharings/sharing_search' %>
</div>

<%= link_to 'Back', student_path(@student) %>