<h1>Listing preferred_addresses</h1>

<table>
  <tr>
    <th>Address</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @preferred_addresses.each do |preferred_address| %>
  <tr>
    <td><%= preferred_address.address_id %></td>
    <td><%= preferred_address.description %></td>
    <td><%= link_to 'Show', preferred_address %></td>
    <td><%= link_to 'Edit', edit_preferred_address_path(preferred_address) %></td>
    <td><%= link_to 'Destroy', preferred_address, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Preferred address', new_preferred_address_path %>
