<%= form_for(@preferred_address) do |f| %>
  <% if @preferred_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@preferred_address.errors.count, "error") %> prohibited this preferred_address from being saved:</h2>

      <ul>
      <% @preferred_address.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :address_id %><br />
    <%= f.text_field :address_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
